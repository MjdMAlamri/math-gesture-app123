[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA-style fallback so Expo Router static pages work everywhere
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
